# IgniteList 项目结构说明

本文档详细说明了 IgniteList 项目的目录结构和代码组织。

## 📁 项目根目录结构

```
igniteList/
├── lib/                    # 📱 Flutter 应用主要代码目录
├── assets/                 # 🎨 资源文件目录（图片、音效等）
├── android/                # 🤖 Android 平台特定代码
├── ios/                    # 🍎 iOS 平台特定代码
├── web/                    # 🌐 Web 平台特定代码
├── windows/                # 🪟 Windows 平台特定代码
├── linux/                  # 🐧 Linux 平台特定代码
├── macos/                  # 🖥️ macOS 平台特定代码
├── test/                   # 🧪 测试代码目录
├── build/                  # 🔨 构建输出目录（自动生成，可忽略）
├── pubspec.yaml            # 📦 项目配置文件（依赖、资源等）
├── pubspec.lock            # 🔒 依赖版本锁定文件（自动生成）
├── analysis_options.yaml   # 🔍 代码分析规则配置
├── README.md               # 📖 项目说明文档
└── PROJECT_STRUCTURE.md    # 📋 项目结构说明（本文件）
```

---

## 📂 详细目录说明

### 1. `lib/` - Flutter 应用核心代码

```
lib/
└── main.dart               # 应用入口文件，包含所有业务逻辑
```

**说明：**
- `main.dart` 是应用的唯一代码文件，包含：
  - 应用入口 (`main()` 函数)
  - 应用配置 (`IgniteListApp`)
  - 待办事项数据模型 (`TodoItem`)
  - 主页面 UI (`TodoListPage`)
  - 所有业务逻辑（添加、完成、删除、保存、加载、每日重置等）

**代码组织：**
- 目前所有代码都在一个文件中（适合小型应用）
- 如果项目扩展，可以考虑拆分为：
  - `models/todo_item.dart` - 数据模型
  - `screens/todo_list_page.dart` - 主页面
  - `services/storage_service.dart` - 存储服务
  - `services/audio_service.dart` - 音效服务
  - `widgets/celebration_overlay.dart` - 庆祝动画组件

---

### 2. `assets/` - 资源文件目录

```
assets/
└── sounds/                 # 音效文件目录
    ├── README.md          # 音效文件说明
    └── success.mp3        # 成功音效（需要用户添加）
```

**说明：**
- 存放应用的静态资源文件
- `sounds/` 目录用于存放音效文件
- 在 `pubspec.yaml` 中已配置资源路径

**使用方式：**
```dart
await _audioPlayer.play(AssetSource('sounds/success.mp3'));
```

---

### 3. `android/` - Android 平台代码

```
android/
├── app/
│   ├── build.gradle.kts   # Android 应用构建配置
│   └── src/
│       ├── main/
│       │   ├── AndroidManifest.xml  # Android 应用清单文件
│       │   ├── kotlin/              # Kotlin 代码
│       │   └── res/                 # Android 资源文件
│       ├── debug/                   # Debug 配置
│       └── profile/                 # Profile 配置
├── build.gradle.kts       # 项目级构建配置
├── gradle/                # Gradle 构建工具
└── settings.gradle.kts    # Gradle 设置
```

**说明：**
- Android 平台特定的配置和代码
- `AndroidManifest.xml` 定义了应用名称、权限等
- 通常不需要修改，除非需要添加特定权限或配置

---

### 4. `ios/` - iOS 平台代码

```
ios/
├── Runner/
│   ├── AppDelegate.swift  # iOS 应用入口
│   ├── Info.plist         # iOS 应用配置
│   ├── Assets.xcassets/   # iOS 资源文件
│   └── Base.lproj/        # 启动界面
├── Runner.xcodeproj/      # Xcode 项目文件
└── Runner.xcworkspace/    # Xcode 工作空间
```

**说明：**
- iOS 平台特定的配置和代码
- `Info.plist` 定义了应用显示名称、权限等
- 通常不需要修改，除非需要添加特定权限或配置

---

### 5. `web/` - Web 平台代码

```
web/
├── index.html             # Web 应用入口 HTML
├── manifest.json          # Web 应用清单
├── favicon.png            # 网站图标
└── icons/                 # Web 应用图标
```

**说明：**
- Web 平台特定的配置
- `index.html` 是 Web 应用的入口文件

---

### 6. `windows/`, `linux/`, `macos/` - 桌面平台代码

**说明：**
- 桌面平台（Windows、Linux、macOS）的特定代码
- 包含平台原生的启动代码和配置
- 通常不需要修改

---

### 7. `test/` - 测试代码

```
test/
└── widget_test.dart       # Widget 测试示例
```

**说明：**
- 存放单元测试和集成测试代码
- 目前只有默认的测试文件
- 可以添加更多测试来验证应用功能

---

### 8. `build/` - 构建输出目录

**说明：**
- Flutter 构建时自动生成的目录
- 包含编译后的代码和资源
- **不需要提交到版本控制**（通常在 `.gitignore` 中）

---

## 📄 重要配置文件

### `pubspec.yaml` - 项目配置文件

**作用：**
- 定义项目名称、版本、描述
- 声明项目依赖（`audioplayers`、`shared_preferences` 等）
- 配置资源文件路径
- 配置应用图标、字体等

**关键配置：**
```yaml
dependencies:
  flutter: ...
  audioplayers: ^6.1.0      # 音效播放
  shared_preferences: ^2.3.2 # 本地存储

flutter:
  assets:
    - assets/sounds/         # 音效资源路径
```

### `analysis_options.yaml` - 代码分析配置

**作用：**
- 配置 Dart 代码分析规则
- 定义代码风格和最佳实践
- 帮助发现潜在问题

---

## 🔄 数据流和代码组织

### 应用启动流程

```
main() 
  ↓
IgniteListApp (应用配置)
  ↓
TodoListPage (主页面)
  ↓
initState() 
  ├─ 初始化动画控制器
  ├─ 注册生命周期监听
  ├─ 加载待办事项 (_loadTodos)
  ├─ 检查每日重置 (_checkAndResetDaily)
  └─ 启动定时器 (_startDailyResetTimer)
```

### 核心功能模块

1. **数据模型** (`TodoItem`)
   - 待办事项的数据结构
   - JSON 序列化/反序列化

2. **数据存储** (`_saveTodos`, `_loadTodos`)
   - 使用 `shared_preferences` 保存到本地
   - 应用启动时自动加载

3. **每日重置** (`_checkAndResetDaily`)
   - 应用启动时检查
   - 应用恢复时检查
   - 定时器每分钟检查

4. **UI 组件**
   - 待办列表显示
   - 输入框和添加按钮
   - 庆祝动画覆盖层

5. **音效播放** (`_playSuccessSound`)
   - 完成待办事项时播放音效

---

## 🎯 代码组织建议（未来扩展）

如果项目需要扩展，建议按以下方式组织：

```
lib/
├── main.dart                    # 应用入口
├── models/
│   └── todo_item.dart          # 数据模型
├── screens/
│   └── todo_list_page.dart     # 主页面
├── widgets/
│   ├── todo_item_widget.dart   # 待办事项组件
│   └── celebration_overlay.dart # 庆祝动画组件
├── services/
│   ├── storage_service.dart    # 存储服务
│   ├── audio_service.dart      # 音效服务
│   └── reset_service.dart      # 重置服务
└── utils/
    └── constants.dart          # 常量定义
```

---

## 📝 总结

**当前项目特点：**
- ✅ 单文件架构，适合小型应用
- ✅ 所有功能集中在 `lib/main.dart`
- ✅ 清晰的平台分离（Android/iOS/Web/Desktop）
- ✅ 资源文件统一管理

**项目规模：**
- 代码文件：1 个主要文件（`lib/main.dart`，约 646 行）
- 资源文件：音效目录（待用户添加）
- 配置文件：标准 Flutter 项目配置

**技术栈：**
- Flutter 3.9.2+
- audioplayers（音效）
- shared_preferences（存储）
- Material Design 3（UI）

---

*最后更新：2025-12-02*

